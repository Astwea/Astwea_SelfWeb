# 如何添加图片和视频到项目

## 📁 文件存放位置

### 图片文件
将图片文件放到：`public/images/` 目录

支持的格式：`.jpg`, `.jpeg`, `.png`, `.webp`, `.gif` 等

### 视频文件
将视频文件放到：`public/videos/` 目录

支持的格式：`.mp4`, `.webm`, `.ogg` 等（推荐使用 `.mp4` 格式）

---

## 📝 在代码中添加图片

打开 `src/pages/Projects.jsx` 文件，找到对应项目的 `images` 数组，添加新的图片对象：

### 图片对象格式

```javascript
{
  src: getAssetPath('/images/你的图片文件名.jpg'),  // 图片路径
  alt: '图片描述（用于无障碍访问）',                // 图片替代文本
  label: '图片标签（显示在图片下方）',              // 图片标签
  clickable: true,                                  // 是否可点击放大（true/false）
}
```

### 示例：添加图片到第一个项目

假设你有一个新图片 `project1-new-image.jpg`，添加到 `public/images/` 目录后：

```javascript
images: [
  {
    src: getAssetPath('/images/project1-before.jpg.jpg'),
    alt: '改装前扫地机器人',
    label: '改装前：碰撞式导航',
    clickable: false,
  },
  {
    src: getAssetPath('/images/project1-pcb.png'),
    alt: 'PCB 设计',
    label: 'PCB 设计',
    clickable: true,
  },
  // 👇 添加新图片
  {
    src: getAssetPath('/images/project1-new-image.jpg'),
    alt: '新功能展示',
    label: '新功能展示',
    clickable: true,
  },
],
```

---

## 🎥 在代码中添加视频

### 视频对象格式

```javascript
video: {
  src: getAssetPath('/videos/你的视频文件名.mp4'),           // 视频文件路径
  thumbnail: getAssetPath('/images/视频缩略图文件名.png'),    // 视频缩略图（可选）
  alt: '视频描述',                                            // 视频替代文本
}
```

### 示例：添加或修改视频

假设你有一个新视频 `project1-new-video.mp4` 和缩略图 `project1-new-video-thumb.png`：

```javascript
video: {
  src: getAssetPath('/videos/project1-new-video.mp4'),
  thumbnail: getAssetPath('/images/project1-new-video-thumb.png'),
  alt: '新功能演示视频',
},
```

**注意**：如果没有缩略图，可以省略 `thumbnail` 字段，系统会使用默认占位符。

---

## 📋 完整示例

### 示例 1：为第一个项目添加更多图片

```javascript
{
  id: 1,
  name: '智能规划控制扫地机器人',
  // ... 其他字段 ...
  images: [
    {
      src: getAssetPath('/images/project1-before.jpg.jpg'),
      alt: '改装前扫地机器人',
      label: '改装前：碰撞式导航',
      clickable: false,
    },
    {
      src: getAssetPath('/images/project1-pcb.png'),
      alt: 'PCB 设计',
      label: 'PCB 设计',
      clickable: true,
    },
    // 添加新图片
    {
      src: getAssetPath('/images/project1-assembly.jpg'),
      alt: '组装过程',
      label: '组装过程',
      clickable: true,
    },
    {
      src: getAssetPath('/images/project1-testing.jpg'),
      alt: '测试场景',
      label: '测试场景',
      clickable: true,
    },
  ],
  video: {
    src: getAssetPath('/videos/project1-sim.mp4'),
    thumbnail: getAssetPath('/images/project1-video-thumb.png'),
    alt: 'Isaac Sim 仿真视频',
  },
}
```

### 示例 2：为项目添加多个视频（需要修改代码结构）

如果项目需要多个视频，可以修改 `video` 为数组：

```javascript
videos: [
  {
    src: getAssetPath('/videos/project1-sim.mp4'),
    thumbnail: getAssetPath('/images/project1-video-thumb.png'),
    alt: 'Isaac Sim 仿真视频',
  },
  {
    src: getAssetPath('/videos/project1-real.mp4'),
    thumbnail: getAssetPath('/images/project1-real-thumb.png'),
    alt: '实机测试视频',
  },
],
```

**注意**：如果使用多个视频，需要修改 `Projects.jsx` 中的渲染逻辑。

---

## ✅ 检查清单

添加图片或视频后，请确认：

- [ ] 文件已正确放置在 `public/images/` 或 `public/videos/` 目录
- [ ] 文件名与代码中的路径一致（注意大小写）
- [ ] 文件扩展名正确（`.jpg`, `.png`, `.mp4` 等）
- [ ] 代码中的路径使用 `getAssetPath()` 函数
- [ ] 图片对象包含 `src`, `alt`, `label`, `clickable` 字段
- [ ] 视频对象包含 `src`, `alt` 字段（`thumbnail` 可选）

---

## 💡 提示

1. **图片命名建议**：使用有意义的文件名，如 `project1-pcb-design.png` 而不是 `image1.png`

2. **图片优化**：
   - 建议使用 WebP 格式以减小文件大小
   - 图片尺寸建议不超过 2000px 宽度
   - 使用图片压缩工具优化文件大小

3. **视频优化**：
   - 视频文件可能较大，建议压缩后再上传
   - 视频时长建议控制在 1-2 分钟以内
   - 使用 H.264 编码的 MP4 格式以获得最佳兼容性

4. **路径说明**：
   - 所有路径都使用 `getAssetPath()` 函数，它会自动处理 GitHub Pages 部署时的路径问题
   - 路径以 `/images/` 或 `/videos/` 开头，不需要包含 `public` 目录名

---

## 🐛 常见问题

### 图片不显示？
- 检查文件路径是否正确
- 确认文件名大小写是否匹配
- 检查文件是否真的存在于 `public/images/` 目录

### 视频无法播放？
- 确认视频格式是否为 `.mp4`
- 检查视频文件是否损坏
- 确认浏览器是否支持该视频格式

### 图片显示变形？
- 图片会自动拉伸到固定尺寸（300px × 220px）
- 如果图片比例不合适，建议先裁剪图片


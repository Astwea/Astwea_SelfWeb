# GitHub Pages 部署说明

## ✅ 已完成的配置

1. ✅ 已安装 `gh-pages` 包
2. ✅ 已配置 `vite.config.js` 的 `base` 路径为 `/SelfWeb/`
3. ✅ 已添加 `deploy` 脚本到 `package.json`

## 🚀 部署步骤

### 方法一：一键部署（推荐）

在项目根目录运行：

```bash
npm run deploy
```

这个命令会：
1. 自动构建项目（`npm run build`）
2. 将 `dist` 目录部署到 GitHub Pages

### 方法二：手动部署

```bash
# 1. 构建项目
npm run build

# 2. 部署到 GitHub Pages
npx gh-pages -d dist
```

## 📝 首次部署后的设置

部署完成后，需要启用 GitHub Pages：

1. **打开 GitHub 仓库**
   - 访问：https://github.com/Astwea/SelfWeb

2. **进入设置**
   - 点击仓库页面的 **"Settings"**（设置）标签

3. **启用 GitHub Pages**
   - 在左侧菜单找到 **"Pages"** 选项
   - 在 **"Source"**（源）下拉菜单中选择 **"gh-pages"** 分支
   - 点击 **"Save"**（保存）

4. **等待部署**
   - GitHub 会在几分钟内完成部署
   - 你会收到邮件通知（如果设置了邮件通知）

## 🌐 访问你的网站

部署完成后，你的网站地址将是：

**https://astwea.github.io/SelfWeb/**

## 🔄 更新网站

每次修改代码后，只需运行：

```bash
npm run deploy
```

然后等待几分钟，GitHub Pages 会自动更新。

## ⚠️ 注意事项

1. **首次部署可能需要 5-10 分钟**才能访问
2. **确保仓库是公开的**（Settings → Change visibility → Make public）
3. **路由问题**：GitHub Pages 会自动处理 React Router 的路由
4. **Base 路径**：如果更改了仓库名，记得修改 `vite.config.js` 中的 `base` 路径

## 🐛 常见问题

### 问题1：404 错误
- **解决**：检查 `vite.config.js` 中的 `base` 路径是否正确
- 确保路径格式为：`/仓库名/`（注意前后都有斜杠）

### 问题2：页面空白
- **解决**：检查浏览器控制台错误
- 确认所有资源路径使用相对路径

### 问题3：路由不工作
- **解决**：GitHub Pages 会自动处理，如果还有问题，检查 `base` 配置

## 📚 相关链接

- GitHub Pages 文档：https://docs.github.com/en/pages
- gh-pages 包文档：https://github.com/tschaub/gh-pages

